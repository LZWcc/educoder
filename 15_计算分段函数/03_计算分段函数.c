#include <stdio.h>
#include <math.h>
main()
{
    float x, y;
    scanf("%f", &x);
    if (x < 0){
        /*********在下面填入正确的语句**********/
        y = powf(1 + x, 2) + 2 * x + 1 / x;
        /*********在下面填入正确的语句**********/
    }
    else{
        /*********在下面填入正确的语句**********/
        y = sqrt(x);
        /*********在下面填入正确的语句**********/
    }
    printf("f(%.2f)=%.2f", x, y);
}